# Components

응집도가 낮고 재사용성이 높은 '순수 UI 컴포넌트'를 모아두는 폴더입니다.
도메인 지식(비즈니스 로직)이 포함되지 않아야 합니다.

## 폴더 구조

- `common/`: Button, Input, Modal, Dropdown 등 기초적인 공통 UI 컴포넌트
- `layout/`: Sidebar, Header, Footer 등 화면의 뼈대를 구성하는 레이아웃 컴포넌트
