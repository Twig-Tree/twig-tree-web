@AGENTS.md
@docs/conventions/fsd-layers.md
@docs/conventions/api-type-boundaries.md
@docs/conventions/typescript-comments.md

## 계획 수행 규칙

- 계획서나 작업 순서를 세운 뒤에는 전체를 한 번에 구현하지 않는다. 한 단계씩 구현 → 확인 → 커밋 → 다음 단계 순서로 진행한다.
- 확인 없이 여러 단계를 이어서 진행하지 않는다. 단계를 마치면 무엇을 했고 다음 단계가 무엇인지 알린 뒤 멈춘다.
- 단계를 진행하다 계획과 다른 판단이 필요해지면 임의로 바꾸지 않고 해당 시점에 사용자에게 알린다.

## Git / GitHub 작업 규칙

- 작업을 마쳤다고 바로 커밋하지 않는다. 무엇을 했는지 알리고 사용자의 확인을 받은 뒤에 커밋한다. 작업 요청이 커밋 승인을 포함하지 않는다.
- 커밋 전에 무엇을 커밋할지(변경 파일 목록)와 커밋 메시지를 함께 알린다.
- `git push`는 사용자가 명시적으로 push를 요청했을 때만 실행한다. 커밋에 대한 승인이 push 승인을 포함하지 않는다 — 커밋과 push는 항상 따로 확인받는다.
- 텍스트 파일의 개행은 LF로 통일한다. `.gitattributes`가 커밋 시점에 강제하고 `.editorconfig`가 에디터를 맞춘다. 파일을 다시 쓰는 도구를 사용할 때는 개행이 바뀌지 않았는지 확인한다 — 개행만 바뀌어도 파일 전체가 diff에 잡혀 실제 변경이 묻힌다.
- `github-pr` skill로 PR을 생성하는 흐름처럼 push가 그 작업 자체에 필수로 포함되는 경우는 예외다. 이때는 PR 생성 승인 시점에 push 여부를 함께 안내하고, 승인 후 별도로 다시 묻지 않는다.
